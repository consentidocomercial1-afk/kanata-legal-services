import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({ subsets: ["latin"] });

export const metadata: Metadata = {
  title: "Kanata Legal Services | Canadian Immigration Law",
  description:
    "Expert Canadian immigration legal services in St. Catharines, Ontario. Work permits, study visas, business immigration, family sponsorship, and refugee claims.",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" className="scroll-smooth">
      <body className={inter.className}>{children}</body>
    </html>
  );
}
